autoconf 
aclocal 
ls << 3
ls << 5
ls << 5
ls << 4
ls <<4
ls << 4
ls $LOGNAME
ls $__CF_USER_TEXT_ENCODING
 $
$HOMEBREW_CACHE
 $ $HOME $$$
aclocal $$
ls $TERM_SESSION_ID
ls $HOME
ls ${
}
ls $TERM_SESSION_ID
ls $USER
ls $XPC_FLAGS
ls $PWD
ls $TERM_PROGRAM
ls $HISTFILESIZE
$LANG
ls $ITERM_PROFILE
mk
ls {$TERM_PROGRAM_VERSION}
ls {$SSH_AUTH_SOCK}
ls {$SSH_AUTH_SOCK}
ls $TERM_PROGRAM
ls ${IFS}
ls ${TMP_DIR}
'
'
grep -R ->sub_prompt ./srcs/
grep -R sub_prompt ./srcs/
ls ${$}
ls $(RE)
ls $(RE)
ls ${TERM_PROGRAM_VERSION}
ls ${ITERM_PROFILE} ${SHELL} $HOME {$
}
{$
}
${COLORTERM}
$Apple_PubSub_Socket_Render
$PS1
${SHELL} ${TERM_PROGRAM_VERSION}
$TMPDIR
${
}
ls $SECURITYSESSIONID
$SECURITYSESSIONID
$COLORFGBG
${PATH}
norminette ./srcs/cmdline/auto_c/
ls ${SHELL}
ls ${{}}


